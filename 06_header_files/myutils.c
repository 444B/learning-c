#include "myutils.h" // Include the header file to match declarations with definitions

// Function definitions
int add(int x, int y) {
    return x + y;
}

int subtract(int x, int y) {
    return x - y;
}
